// Demo data is now seeded automatically the first time auth-service and
// shipments-service start (see their src/index.ts). You don't need to run
// anything here — just `npm install && npm run dev`.
//
// To start over with fresh demo data, stop the servers and run:
//     npm run reset
// then `npm run dev` again.

export {};
